// use steamworks::{Client, AppId};

#[allow(dead_code)]
fn initialize_steamworks(_app_id: i64) {
    // let (client, single) match = Client::init_app(AppId(app_id.clone())) {
    //     Ok(result) => result,
    //     Err(e) => {
    //         println!("Error initializing Steamworks: {}", e);
    //         return;
    //     }
    // };
    //
    // todo: Initialize Steamworks and handle callbacks
    // single.run_callbacks();
}