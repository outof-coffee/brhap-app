[package]
name = "brhap-integrations"
version = "0.1.0"
edition = "2024"
publish = false

[dependencies]

[dev-dependencies]
brhap-server = { path = "../brhap-server" }
steam-vdf-parser = "0.1"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
