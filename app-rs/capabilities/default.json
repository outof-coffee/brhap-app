{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Core permissions for the brhap window. No plugins, since every capability the app needs is a command backed by the server-rs library.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
